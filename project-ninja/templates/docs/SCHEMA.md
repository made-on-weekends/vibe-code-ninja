# SCHEMA.md

> Table-by-table reference. High-level strategy lives in `DATABASE.md`.

## Conventions

- Primary keys: TODO(<owner>): e.g., `bigint generated by default as identity`, `uuid`, etc.
- Timestamps: TODO(<owner>): typical pattern (e.g., `created_at`, `updated_at` with default `now()`)
- Tenant column (if multi-tenant): TODO(<owner>): name and indexing/policy expectations
- Naming: TODO(<owner>): casing for tables/columns, plural/singular policy
- PII columns: marked with `[PII]` in the Notes column. Inventory owned by `SECURITY.md`.

## Tables

### `tenants`
TODO(<owner>): Replace with actual tables.

| Column      | Type        | Notes               |
|-------------|-------------|---------------------|
| id          | bigint PK   |                     |
| name        | text        | not null            |
| created_at  | timestamptz | default `now()`     |

Indexes: TODO(<owner>)
Foreign keys: TODO(<owner>)
Access policies: TODO(<owner>)

### `users`
TODO(<owner>): same shape

---

## Relationship overview

```
TODO(<owner>): Optional ASCII ERD or short prose summary of how the major tables relate.
```

## Notable indexes

TODO(<owner>): Indexes worth knowing about (composite, partial, GiST/GIN).

## Generated/derived data

TODO(<owner>): Any materialized views, triggers, computed columns, or generated columns. Note what generates them.
